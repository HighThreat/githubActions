{
  "name": "githubActions-secure-deployment-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "test": "node --test",
    "ci": "npm run build && npm test"
  }
}